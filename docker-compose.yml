version: '2.1'
services:
  authentication-service:
    image: /authentication-service
    restart: always
    ports:
    - "8899:8899"
    links:
    - main-service

  main-service:
    image: /main-service
    restart: always
    links:
    - authentication-service

