'use strict';

describe('myApp.transports module', function() {

  beforeEach(module('myApp.transports'));

  describe('transports controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('TransportsCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});